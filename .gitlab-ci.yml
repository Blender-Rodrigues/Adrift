stages:
  - test

test:
  tags:
    - iti0200
  stage: test
  image: openjdk:8-jdk-alpine
  script:
    - chmod 700 gradlew
    - ./gradlew clean test build --stacktrace
  artifacts:
    paths:
      - build/reports/
      - build/test-results/
      - build/libs/
