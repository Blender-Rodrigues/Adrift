stages:
  - test

test:
  tags:
    - iti0200
  stage: test
  image: openjdk:8-jdk-alpine
  script:
    - chmod 700 gradlew
    - export NO_GUI=true
    - ./gradlew clean test build
  artifacts:
    paths:
      - build/reports/
      - build/test-results/
      - build/libs/
